<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- ============================= -->
    <!-- 1. Console Appender           -->
    <!-- ============================= -->
<!--    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">-->
<!--        <encoder>-->
<!--            &lt;!&ndash; Layout / Pattern &ndash;&gt;-->
<!--            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] %logger{36} - %msg%n</pattern>-->
<!--        </encoder>-->
<!--    </appender>-->


    <!-- ============================= -->
    <!-- 2. Rolling File Appender      -->
    <!-- ============================= -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>
            /tmp/logs/e_shoppers.log
        </file>

        <!-- Log rollover rules -->
        <rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
            <!-- daily log files -->
            <fileNamePattern>
                /tmp/logs/e_shoppers.log%i.zip
            </fileNamePattern>
            <minIndex>1</minIndex>
            <maxIndex>20</maxIndex>
<!--            <maxHistory>7</maxHistory> &lt;!&ndash; keep logs for 7 days &ndash;&gt;-->
        </rollingPolicy>
        <triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
            <maxFileSize>1KB</maxFileSize>
        </triggeringPolicy>

        <encoder>
            <!-- Layout / Pattern -->
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %magenta([%thread]) %highlight(%-5level) %logger{36}.%M - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ============================= -->
    <!-- STDOUT Console Appender       -->
    <!-- ============================= -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %magenta([%thread]) %highlight(%-5level) %logger{36}.%M - %msg%n</pattern>
        </encoder>
        <target>System.out</target>
    </appender>

    <!-- ============================= -->
    <!-- 3. Root Logger Configuration  -->
    <!-- ============================= -->
    <root level="DEBUG">
        <appender-ref ref="STDOUT" />
<!--        <appender-ref ref="CONSOLE" />-->
        <appender-ref ref="FILE" />
    </root>

</configuration>